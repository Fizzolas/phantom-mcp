# Phantom MCP — Python dependencies
#
# TIERS:
#   requirements-core.txt  <- install this first; server starts with these alone
#   requirements.txt       <- full install (includes optional heavy deps)
#
# Install core only (recommended for first-time setup):
#   pip install -r requirements-core.txt
#
# Install everything:
#   pip install -r requirements.txt
#   python -m playwright install chromium

# ---- CORE (required — server will NOT start without these) -----------------
mcp>=1.0.0
pydantic>=2.7.0
httpx>=0.27.0
python-dotenv>=1.0.0

# ---- DATA (optional — needed by desktop_find_text and memory tools) ---------
numpy>=1.26.4
pandas>=2.2.0
scikit-learn>=1.4.2

# ---- DESKTOP AUTOMATION (optional — disables desktop_* tools if missing) ---
pyautogui>=0.9.54
pygetwindow>=0.0.9
pyscreenshot>=0.4.2
mss>=9.0.1
psutil>=5.9.8
pynput>=1.7.6
Pillow>=10.3.0

# ---- NOTIFICATIONS (optional — Windows only) --------------------------------
winotify>=1.1.0

# ---- WEB / SEARCH — headless Chromium (optional) ---------------------------
playwright>=1.44.0

# ---- OCR (optional — also requires Tesseract binary on PATH) ----------------
# NOTE: rapidocr-onnxruntime does not support Python 3.14+
# If you are on Python 3.13 or earlier, you can install it manually:
#   pip install "rapidocr-onnxruntime>=1.3.24"
# pytesseract works on all Python versions and is installed below.
pytesseract>=0.3.10

# ---- VISION / OBJECT DETECTION (optional) -----------------------------------
opencv-python>=4.9.0

# ---- VIDEO TRANSCRIPTION (optional) -----------------------------------------
faster-whisper>=1.0.3
yt-dlp>=2024.5.27

# ---- DOCUMENT READING (optional) --------------------------------------------
pdfminer.six>=20221105
python-docx>=1.1.2
docx2txt>=0.8

# ---- QR CODE (optional) -----------------------------------------------------
qrcode[pil]>=7.4.2

# ---- CLOUD STORAGE (optional) -----------------------------------------------
boto3>=1.34.0

# ---- RSS / ATOM FEED (optional) ---------------------------------------------
feedparser>=6.0.11

# ---- WEB SERVER (optional — not used by default MCP stdio mode) -------------
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
